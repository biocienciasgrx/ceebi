import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.hipycas.ceebi',
  appName: 'CEEBI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
